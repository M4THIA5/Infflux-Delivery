meta {
  name: Accept Course
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/courses/:id/accept
  body: json
  auth: inherit
}

params:path {
  id: {{courseId}}
}

body:json {
  {
    "delivererId": "{{userId}}"
  }
}
