meta {
  name: Accept Course
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/courses/:id/accept
  body: json
  auth: inherit
}

params:path {
  id: {{courseId}}
}

body:json {
  {
    "delivererId": "{{userId}}"
  }
}
